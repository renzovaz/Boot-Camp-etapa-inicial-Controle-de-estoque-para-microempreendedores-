name: CI

on:
  push:
    branches: ["main", "develop", "entrega-intermediaria"]
  pull_request:
    branches: ["main"]

jobs:
  lint-and-test:
    name: Lint & Testes
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint com Ruff
        run: ruff check src/ tests/

      - name: Verificar formatação com Ruff
        run: ruff format --check src/ tests/

      - name: Testes unitários e de integração (mock)
        run: pytest --cov=src --cov-report=term-missing -v

      # Testes de integração REAL (apenas na branch main após merge)
      - name: Testes de integração real com ViaCEP
        if: github.ref == 'refs/heads/main'
        run: pytest -m integration tests/test_suppliers_integration.py -v
        continue-on-error: true   # Não quebra o CI se a API estiver instável